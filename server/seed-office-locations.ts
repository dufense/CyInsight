// Office locations are now user-defined via My Organization → Infrastructure.
// This file is intentionally a no-op — no hardcoded seed data.

export async function seedOfficeLocations(): Promise<void> {
  // No-op: office locations must be configured by the user through the UI.
}
