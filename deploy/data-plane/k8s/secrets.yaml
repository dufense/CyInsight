apiVersion: v1
kind: Secret
metadata:
  name: secureops-secrets
  namespace: secureops
  labels:
    app: secureops
    plane: data
type: Opaque
stringData:
  timescaledb-url: "REPLACE_WITH_ACTUAL_URL"
  openai-api-key: "REPLACE_WITH_ACTUAL_KEY"
