global:
  storageClass: "gp3"
  dataResidency:
    country: BH
    label: bahrain
    complianceFramework: PDPL-Bahrain

cloudProvider: "aws"

cloudProviderConfig:
  name: AWS
  zones:
    - me-south-1a
    - me-south-1b
    - me-south-1c

receiver:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 10
  pdb:
    enabled: true
    minAvailable: 1
  ingress:
    host: bh-ingest.secureops.io
    tls:
      secretName: secureops-bh-receiver-tls
  config:
    region: bahrain
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: topology.kubernetes.io/zone
                operator: In
                values:
                  - me-south-1a
                  - me-south-1b
                  - me-south-1c

management:
  enabled: false

dataPlane:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 6
  pdb:
    enabled: true
    minAvailable: 1
  config:
    region: bahrain
    objectStorageBucket: secureops-events-bahrain
    objectStorageRegion: me-south-1

collector:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 6
  pdb:
    enabled: true
    minAvailable: 1

normalizer:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 8
  pdb:
    enabled: true
    minAvailable: 1

detectionEngine:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 6
  pdb:
    enabled: true
    minAvailable: 1

enrichment:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 4
  pdb:
    enabled: true
    minAvailable: 1

storage:
  replicaCount: 2
  hpa:
    minReplicas: 2
    maxReplicas: 4
  pdb:
    enabled: true
    minAvailable: 1

kafka:
  enabled: false

kafkaKraft:
  enabled: true
  replicaCount: 3
  numPartitions: 12
  defaultReplicationFactor: 3
  minInsyncReplicas: 2
  persistence:
    size: 50Gi
  pdb:
    enabled: true
    minAvailable: 2

objectStorage:
  provider: s3
  bucket: secureops-events-bahrain
  region: me-south-1
  storageClass: STANDARD
  archiveStorageClass: GLACIER
